# Pinned dependencies for the Finnish->English translation benchmark — CUDA / GPU.
#
# Use this file instead of requirements.txt on a machine with an NVIDIA GPU:
#     python3 -m venv .venv
#     .venv/bin/python -m pip install -r requirements-cuda.txt
#     .venv/bin/python benchmark.py        # auto-detects and uses the GPU
#
# This mirrors requirements.txt exactly except for torch: it pins the CUDA build
# (torch==2.12.1+cu126) from PyTorch's cu126 index instead of the CPU build.
# Keep the (non-torch) pins below in sync with requirements.txt. After editing
# requirements.txt, regenerate the dependency block below with:
#     grep -E '^[A-Za-z0-9_.-]+==' requirements.txt | sed 's/^torch==.*/torch==2.12.1+cu126/'
#
# CUDA version: torch 2.12.1 ships cu126 (CUDA 12.6), cu129, and cu130 builds.
# cu126 is chosen here for the broadest NVIDIA-driver compatibility. To use a
# newer toolkit, change BOTH the --extra-index-url tag and the +cuXXX pin below
# to cu129 or cu130 (needs a newer driver). See https://pytorch.org/get-started/locally/.
# The CUDA build also pulls in nvidia-* runtime packages; torch pins their exact
# versions, so they resolve deterministically and need not be listed here.
--extra-index-url https://download.pytorch.org/whl/cu126

annotated-doc==0.0.4
anyio==4.14.1
certifi==2026.6.17
click==8.4.2
filelock==3.29.4
fsspec==2026.6.0
h11==0.16.0
hf-xet==1.5.1
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.20.1
idna==3.18
Jinja2==3.1.6
joblib==1.5.3
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
mpmath==1.3.0
networkx==3.4.2
numpy==2.2.6
packaging==26.2
Pygments==2.20.0
PyYAML==6.0.3
regex==2026.5.9
rich==15.0.0
sacremoses==0.1.1
safetensors==0.8.0
sentencepiece==0.2.1
setuptools==81.0.0
shellingham==1.5.4
sympy==1.14.0
tokenizers==0.22.2
torch==2.12.1+cu126
tqdm==4.68.3
transformers==5.12.1
typer==0.25.1
typing_extensions==4.15.0
