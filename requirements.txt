# Pinned dependencies for the Finnish->English translation benchmark.
#
# Install on another machine:
#     python3 -m venv .venv
#     .venv/bin/python -m pip install -r requirements.txt
#
# torch is pinned to the CPU build (torch==2.12.1+cpu), which lives on
# PyTorch's own index rather than PyPI. The --extra-index-url line below makes
# `pip install -r requirements.txt` resolve it automatically.
#
# On a machine with an NVIDIA GPU, install requirements-cuda.txt instead to get
# a CUDA-enabled torch. benchmark.py auto-detects the GPU at runtime and uses it
# (falling back to CPU if none is present).
--extra-index-url https://download.pytorch.org/whl/cpu

annotated-doc==0.0.4
anyio==4.14.1
certifi==2026.6.17
click==8.4.2
filelock==3.29.4
fsspec==2026.6.0
h11==0.16.0
hf-xet==1.5.1
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.20.1
idna==3.18
Jinja2==3.1.6
joblib==1.5.3
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
mpmath==1.3.0
networkx==3.6.1
numpy==2.5.0
packaging==26.2
Pygments==2.20.0
PyYAML==6.0.3
regex==2026.5.9
rich==15.0.0
sacremoses==0.1.1
safetensors==0.8.0
sentencepiece==0.2.1
setuptools==81.0.0
shellingham==1.5.4
sympy==1.14.0
tokenizers==0.22.2
torch==2.12.1+cpu
tqdm==4.68.3
transformers==5.12.1
typer==0.25.1
typing_extensions==4.15.0
